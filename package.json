{
  "name": "fraudshield-monorepo",
  "version": "1.0.0",
  "description": "FraudShield Real-Time FinTech Fraud Detection & Risk Engine Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@fraudshield/api",
    "dev:web": "npm run dev --workspace=@fraudshield/web",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:web\"",
    "build": "npm run build --workspace=@fraudshield/shared-types && npm run build --workspace=@fraudshield/api && npm run build --workspace=@fraudshield/web && node -e \"require('fs').cpSync('apps/web/dist', 'dist', {recursive: true})\"",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "autoprefixer": "^10.5.5",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "postcss": "^8.5.28",
    "tailwindcss": "^4.3.3",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
